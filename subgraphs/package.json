{
  "name": "pop-subgraph",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build --network mumbai",
    "build-tbsc": "graph build --network tbsc",
    "deploy": "graph deploy --product hosted-service smrnjeet222/pop-v2",
    "deploy-tbsc": "graph deploy pop-v2 --node http://localhost:8020/ --ipfs http://localhost:5001",
    "test": "graph test",
    "create": "graph create pop-v2 --node http://localhost:8020/ ",
    "remove": "graph remove pop-v2 --node http://localhost:8020/ "
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.50.1",
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}